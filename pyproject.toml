[project]
name = "vps-romero"
version = "0.1.0"
description = "Declarative configuration for the romero.lolma.us VPS"
requires-python = ">=3.12,<3.15"
dependencies = [
  "ansible-core==2.21.3",
  "ansible-lint==26.8.0",
]

[tool.uv]
package = false
